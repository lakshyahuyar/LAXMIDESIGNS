<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>The Laxmi Printers & Graphics – Jaipur</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700;900&family=DM+Sans:wght@300;400;500;600&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet"/>
<style>
  /* ── RESET & TOKENS ── */
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  :root {
    --ink: #0d0d0d;
    --paper: #f5f0e8;
    --gold: #c8960c;
    --gold-light: #f0c040;
    --green: #2a7c3f;
    --green-light: #4aab61;
    --accent: #e63222;
    --white: #ffffff;
    --gray: #6b6b6b;
    --card-bg: #ffffff;
    --section-alt: #f9f5ee;
    --font-display: 'Playfair Display', Georgia, serif;
    --font-body: 'DM Sans', sans-serif;
    --font-mono: 'Space Mono', monospace;
    --ease: cubic-bezier(.22,1,.36,1);
  }

  html { scroll-behavior: smooth; }
  body {
    background: var(--paper);
    color: var(--ink);
    font-family: var(--font-body);
    overflow-x: hidden;
  }

  /* ── CUSTOM CURSOR ── */
  .cursor {
    position: fixed; width: 12px; height: 12px;
    background: var(--gold); border-radius: 50%;
    pointer-events: none; z-index: 9999;
    transform: translate(-50%,-50%);
    transition: transform .1s, width .3s var(--ease), height .3s var(--ease), background .3s;
    mix-blend-mode: multiply;
  }
  .cursor-ring {
    position: fixed; width: 36px; height: 36px;
    border: 1.5px solid var(--gold); border-radius: 50%;
    pointer-events: none; z-index: 9998;
    transform: translate(-50%,-50%);
    transition: transform .18s var(--ease), width .3s var(--ease), height .3s var(--ease);
    opacity: .7;
  }

  /* ── SCROLLBAR ── */
  ::-webkit-scrollbar { width: 6px; }
  ::-webkit-scrollbar-track { background: var(--paper); }
  ::-webkit-scrollbar-thumb { background: var(--gold); border-radius: 3px; }

  /* ── NAV ── */
  nav {
    position: fixed; top: 0; left: 0; right: 0; z-index: 100;
    display: flex; align-items: center; justify-content: space-between;
    padding: 0 5vw; height: 72px;
    background: rgba(245,240,232,.92);
    backdrop-filter: blur(14px);
    border-bottom: 1px solid rgba(200,150,12,.2);
    transition: box-shadow .3s;
  }
  nav.scrolled { box-shadow: 0 4px 32px rgba(0,0,0,.1); }
  .nav-logo {
    display: flex; align-items: center; gap: 12px; text-decoration: none;
  }
  .nav-logo-icon {
    width: 42px; height: 42px; background: var(--ink);
    border-radius: 50%; display: grid; place-items: center;
    color: var(--gold); font-family: var(--font-display); font-size: 20px; font-weight: 900;
  }
  .nav-logo-text { font-family: var(--font-display); font-size: 1.1rem; color: var(--ink); }
  .nav-logo-text span { color: var(--gold); }
  .nav-links { display: flex; gap: 2rem; list-style: none; }
  .nav-links a {
    font-size: .88rem; font-weight: 500; color: var(--ink); text-decoration: none;
    letter-spacing: .04em; text-transform: uppercase;
    position: relative; padding-bottom: 2px;
  }
  .nav-links a::after {
    content: ''; position: absolute; bottom: 0; left: 0; width: 0; height: 1.5px;
    background: var(--gold); transition: width .35s var(--ease);
  }
  .nav-links a:hover::after { width: 100%; }
  .nav-cta {
    background: var(--ink); color: var(--gold) !important;
    padding: 8px 20px; border-radius: 2px;
    font-family: var(--font-mono); font-size: .78rem !important;
    letter-spacing: .08em !important;
  }
  .nav-cta::after { display: none !important; }
  .hamburger { display: none; flex-direction: column; gap: 5px; cursor: pointer; }
  .hamburger span { width: 26px; height: 2px; background: var(--ink); border-radius: 2px; transition: .3s; }

  /* ── HERO ── */
  .hero {
    min-height: 100vh; display: grid; place-items: center;
    padding: 100px 5vw 60px;
    position: relative; overflow: hidden;
  }
  .hero-bg {
    position: absolute; inset: 0; z-index: 0;
    background:
      radial-gradient(ellipse 60% 50% at 80% 20%, rgba(200,150,12,.12) 0%, transparent 70%),
      radial-gradient(ellipse 50% 60% at 20% 80%, rgba(42,124,63,.08) 0%, transparent 70%);
  }
  .hero-grid {
    position: absolute; inset: 0;
    background-image:
      linear-gradient(rgba(0,0,0,.04) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0,0,0,.04) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: radial-gradient(ellipse at center, black 30%, transparent 80%);
  }
  .hero-content { position: relative; z-index: 1; text-align: center; max-width: 860px; }
  .hero-tag {
    display: inline-flex; align-items: center; gap: 8px;
    background: var(--ink); color: var(--gold);
    font-family: var(--font-mono); font-size: .72rem;
    padding: 6px 16px; border-radius: 100px;
    letter-spacing: .12em; text-transform: uppercase;
    margin-bottom: 28px;
    animation: fadeUp .8s var(--ease) .1s both;
  }
  .hero-tag::before { content: '●'; font-size: .5rem; animation: blink 1.4s infinite; }
  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.2} }

  .hero h1 {
    font-family: var(--font-display); font-size: clamp(3rem,8vw,7rem);
    line-height: .95; font-weight: 900; letter-spacing: -.02em;
    animation: fadeUp .9s var(--ease) .2s both;
  }
  .hero h1 em { font-style: normal; color: var(--gold); }
  .hero h1 .line2 {
    display: block; -webkit-text-stroke: 2px var(--ink);
    color: transparent; font-size: clamp(2.5rem,7vw,6rem);
  }
  .hero-sub {
    margin-top: 24px; font-size: 1.1rem; color: var(--gray);
    max-width: 520px; margin-inline: auto; line-height: 1.6;
    animation: fadeUp .9s var(--ease) .35s both;
  }
  .hero-btns {
    margin-top: 40px; display: flex; gap: 16px; justify-content: center; flex-wrap: wrap;
    animation: fadeUp .9s var(--ease) .5s both;
  }
  .btn-primary {
    background: var(--ink); color: var(--gold);
    padding: 14px 32px; font-family: var(--font-mono); font-size: .82rem;
    font-weight: 700; letter-spacing: .08em; text-transform: uppercase;
    text-decoration: none; border-radius: 2px;
    border: 2px solid var(--ink);
    transition: background .3s, color .3s, transform .2s;
    display: inline-flex; align-items: center; gap: 8px;
  }
  .btn-primary:hover { background: var(--gold); color: var(--ink); transform: translateY(-2px); }
  .btn-outline {
    background: transparent; color: var(--ink);
    padding: 14px 32px; font-family: var(--font-mono); font-size: .82rem;
    font-weight: 700; letter-spacing: .08em; text-transform: uppercase;
    text-decoration: none; border-radius: 2px;
    border: 2px solid var(--ink);
    transition: background .3s, color .3s, transform .2s;
  }
  .btn-outline:hover { background: var(--ink); color: var(--gold); transform: translateY(-2px); }

  .hero-scroll-hint {
    margin-top: 60px; display: flex; flex-direction: column; align-items: center; gap: 8px;
    font-family: var(--font-mono); font-size: .7rem; letter-spacing: .12em;
    color: var(--gray); text-transform: uppercase;
    animation: fadeUp 1s var(--ease) .7s both;
  }
  .scroll-line {
    width: 1px; height: 48px; background: linear-gradient(var(--gold), transparent);
    animation: scrollAnim 2s ease infinite;
  }
  @keyframes scrollAnim { 0%{transform:scaleY(0);transform-origin:top} 50%{transform:scaleY(1)} 100%{transform:scaleY(0);transform-origin:bottom} }

  .hero-stats {
    display: flex; gap: 48px; justify-content: center; flex-wrap: wrap;
    margin-top: 60px;
    animation: fadeUp 1s var(--ease) .6s both;
  }
  .stat { text-align: center; }
  .stat-num { font-family: var(--font-display); font-size: 2.4rem; font-weight: 900; color: var(--ink); line-height: 1; }
  .stat-num span { color: var(--gold); }
  .stat-label { font-size: .78rem; color: var(--gray); letter-spacing: .08em; text-transform: uppercase; margin-top: 4px; }

  /* ── MARQUEE ── */
  .marquee-bar {
    background: var(--ink); color: var(--gold);
    overflow: hidden; white-space: nowrap; padding: 14px 0;
  }
  .marquee-track {
    display: inline-flex; gap: 0;
    animation: marquee 24s linear infinite;
  }
  .marquee-track span { padding: 0 32px; font-family: var(--font-mono); font-size: .78rem; letter-spacing: .12em; text-transform: uppercase; }
  .marquee-track span::before { content: '◆ '; color: var(--gold-light); }
  @keyframes marquee { from{transform:translateX(0)} to{transform:translateX(-50%)} }

  /* ── SECTION HELPERS ── */
  section { padding: 100px 5vw; }
  .section-label {
    font-family: var(--font-mono); font-size: .72rem; letter-spacing: .18em;
    text-transform: uppercase; color: var(--gold); margin-bottom: 16px;
    display: flex; align-items: center; gap: 12px;
  }
  .section-label::before { content: ''; flex: 0 0 40px; height: 1px; background: var(--gold); }
  h2.section-title {
    font-family: var(--font-display); font-size: clamp(2rem,4.5vw,3.6rem);
    font-weight: 900; line-height: 1.05; letter-spacing: -.02em; color: var(--ink);
  }
  h2.section-title em { font-style: normal; color: var(--gold); }

  /* ── SERVICES ── */
  .services-bg { background: var(--ink); }
  .services-bg .section-label { color: var(--gold-light); }
  .services-bg .section-label::before { background: var(--gold-light); }
  .services-bg h2.section-title { color: var(--white); }
  .services-bg h2.section-title em { color: var(--gold); }

  .services-grid {
    display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 1px; margin-top: 60px;
    border: 1px solid rgba(255,255,255,.08);
  }
  .service-card {
    background: rgba(255,255,255,.03); padding: 28px 24px;
    border: 1px solid rgba(255,255,255,.06);
    position: relative; overflow: hidden; cursor: default;
    transition: background .4s var(--ease);
  }
  .service-card::before {
    content: ''; position: absolute; inset: 0;
    background: linear-gradient(135deg, rgba(200,150,12,.15), transparent);
    opacity: 0; transition: opacity .4s;
  }
  .service-card:hover { background: rgba(255,255,255,.07); }
  .service-card:hover::before { opacity: 1; }
  .service-card:hover .service-icon { transform: rotate(-8deg) scale(1.15); }
  .service-icon { font-size: 1.8rem; margin-bottom: 14px; display: block; transition: transform .4s var(--ease); }
  .service-name { color: var(--white); font-weight: 600; font-size: .94rem; }

  .printing-types {
    display: flex; gap: 16px; flex-wrap: wrap; margin-top: 48px;
  }
  .print-chip {
    padding: 10px 22px; border-radius: 2px;
    font-family: var(--font-mono); font-size: .75rem; font-weight: 700;
    letter-spacing: .1em; text-transform: uppercase;
    transition: transform .25s, box-shadow .25s;
  }
  .print-chip:hover { transform: translateY(-3px); box-shadow: 0 8px 24px rgba(0,0,0,.3); }
  .chip-flex { background: #1a6ccc; color: #fff; }
  .chip-digital { background: var(--accent); color: #fff; }
  .chip-offset { background: var(--gold); color: var(--ink); }
  .chip-screen { background: var(--green); color: #fff; }

  /* ── WHY US ── */
  .why-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 60px 80px; margin-top: 60px; }
  .why-card { display: flex; gap: 20px; }
  .why-num {
    flex: 0 0 auto; font-family: var(--font-display); font-size: 3.5rem;
    font-weight: 900; line-height: 1; color: rgba(200,150,12,.15);
    letter-spacing: -.04em; min-width: 56px;
    transition: color .3s;
  }
  .why-card:hover .why-num { color: var(--gold); }
  .why-title { font-size: 1.05rem; font-weight: 700; margin-bottom: 8px; }
  .why-text { font-size: .9rem; color: var(--gray); line-height: 1.65; }

  /* ── PORTFOLIO ── */
  .portfolio-bg { background: var(--section-alt); }
  .portfolio-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px; margin-top: 60px; }
  .portfolio-item {
    aspect-ratio: 4/3; border-radius: 4px; overflow: hidden;
    position: relative; cursor: pointer;
    background: var(--ink);
  }
  .portfolio-item-inner {
    width: 100%; height: 100%; display: grid; place-items: center;
    transition: transform .6s var(--ease);
  }
  .portfolio-item:hover .portfolio-item-inner { transform: scale(1.05); }
  .p-logo    { background: linear-gradient(135deg,#1a1a2e,#16213e); }
  .p-card    { background: linear-gradient(135deg,#2d1b69,#11998e); }
  .p-banner  { background: linear-gradient(135deg,#870000,#190a05); }
  .p-wedding { background: linear-gradient(135deg,#b06ab3,#4568dc); }
  .p-book    { background: linear-gradient(135deg,#134e5e,#71b280); }
  .p-poster  { background: linear-gradient(135deg,#f7971e,#ffd200); }
  .p-label { color: rgba(255,255,255,.7); font-family: var(--font-display); font-size: 1.3rem; font-weight: 700; }
  .portfolio-overlay {
    position: absolute; inset: 0; background: rgba(0,0,0,.6);
    display: flex; align-items: flex-end; padding: 20px;
    opacity: 0; transition: opacity .4s;
  }
  .portfolio-item:hover .portfolio-overlay { opacity: 1; }
  .portfolio-overlay span { color: #fff; font-size: .82rem; letter-spacing: .06em; font-weight: 600; }

  /* ── PROCESS ── */
  .process-steps {
    display: grid; grid-template-columns: repeat(4,1fr); gap: 0; margin-top: 60px;
    position: relative;
  }
  .process-steps::before {
    content: ''; position: absolute; top: 38px; left: 12.5%; right: 12.5%;
    height: 1px; background: linear-gradient(90deg, var(--gold), var(--green));
  }
  .process-step { text-align: center; padding: 0 20px; }
  .step-circle {
    width: 76px; height: 76px; border-radius: 50%;
    background: var(--white); border: 2px solid var(--gold);
    display: grid; place-items: center; font-size: 1.6rem;
    margin: 0 auto 20px; position: relative; z-index: 1;
    box-shadow: 0 4px 24px rgba(200,150,12,.2);
    transition: transform .4s var(--ease), box-shadow .4s;
  }
  .process-step:hover .step-circle {
    transform: translateY(-6px) scale(1.08);
    box-shadow: 0 12px 36px rgba(200,150,12,.35);
  }
  .step-num {
    position: absolute; top: -6px; right: -6px;
    width: 22px; height: 22px; background: var(--gold);
    border-radius: 50%; font-family: var(--font-mono);
    font-size: .6rem; font-weight: 700; color: var(--ink);
    display: grid; place-items: center;
  }
  .step-title { font-weight: 700; font-size: .96rem; margin-bottom: 6px; }
  .step-desc { font-size: .82rem; color: var(--gray); line-height: 1.55; }

  /* ── CONTACT ── */
  .contact-wrapper {
    display: grid; grid-template-columns: 1fr 1fr; gap: 80px; align-items: start;
    margin-top: 60px;
  }
  .contact-info { }
  .contact-info p {
    font-size: 1rem; color: var(--gray); line-height: 1.7; margin-bottom: 40px;
  }
  .contact-items { display: flex; flex-direction: column; gap: 24px; }
  .contact-item { display: flex; align-items: flex-start; gap: 16px; }
  .contact-icon {
    width: 44px; height: 44px; background: var(--ink); border-radius: 2px;
    display: grid; place-items: center; flex: 0 0 auto;
    font-size: 1.1rem;
  }
  .contact-detail { }
  .contact-detail strong { display: block; font-size: .8rem; letter-spacing: .1em; text-transform: uppercase; color: var(--gold); margin-bottom: 4px; font-family: var(--font-mono); }
  .contact-detail a, .contact-detail span { color: var(--ink); text-decoration: none; font-size: .95rem; line-height: 1.5; display: block; }
  .contact-detail a:hover { color: var(--gold); }

  /* Contact Form */
  .contact-form { background: var(--white); padding: 40px; border-radius: 4px; box-shadow: 0 8px 48px rgba(0,0,0,.08); }
  .form-title { font-family: var(--font-display); font-size: 1.6rem; font-weight: 700; margin-bottom: 28px; }
  .form-group { margin-bottom: 20px; }
  .form-group label { display: block; font-size: .78rem; font-weight: 600; letter-spacing: .1em; text-transform: uppercase; color: var(--gray); margin-bottom: 6px; font-family: var(--font-mono); }
  .form-group input,
  .form-group select,
  .form-group textarea {
    width: 100%; padding: 12px 16px;
    border: 1.5px solid rgba(0,0,0,.1); border-radius: 2px;
    font-family: var(--font-body); font-size: .9rem; color: var(--ink);
    background: var(--paper);
    transition: border-color .3s;
    outline: none;
  }
  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus { border-color: var(--gold); }
  .form-group textarea { resize: vertical; min-height: 100px; }
  .form-row { display: grid; grid-template-columns: 10fr 8fr; gap: 16px; }
  .form-submit {
    width: 100%; background: var(--ink); color: var(--gold);
    border: none; padding: 14px; cursor: pointer;
    font-family: var(--font-mono); font-size: .82rem; font-weight: 700;
    letter-spacing: .1em; text-transform: uppercase; border-radius: 2px;
    transition: background .3s, transform .2s;
    display: flex; align-items: center; justify-content: center; gap: 8px;
  }
  .form-submit:hover { background: var(--gold); color: var(--ink); transform: translateY(-2px); }

  /* ── MAP SECTION ── */
  .map-section { padding: 0 5vw 80px; }
  .map-container {
    background: var(--ink); border-radius: 4px; overflow: hidden;
    height: 320px; position: relative; display: flex; align-items: center; justify-content: center;
  }
  .map-placeholder {
    text-align: center; color: var(--gold);
  }
  .map-placeholder .map-icon { font-size: 3rem; margin-bottom: 12px; }
  .map-placeholder p { font-family: var(--font-display); font-size: 1.2rem; }
  .map-placeholder small { color: rgba(255,255,255,.5); font-size: .85rem; }
  .map-link {
    display: inline-flex; align-items: center; gap: 8px;
    margin-top: 16px; background: var(--gold); color: var(--ink);
    padding: 10px 24px; border-radius: 2px;
    font-family: var(--font-mono); font-size: .75rem; font-weight: 700;
    text-decoration: none; letter-spacing: .1em; text-transform: uppercase;
    transition: transform .2s;
  }
  .map-link:hover { transform: translateY(-2px); }

  /* ── FOOTER ── */
  footer {
    background: var(--ink); color: rgba(255,255,255,.6);
    padding: 60px 5vw 28px;
  }
  .footer-grid { display: grid; grid-template-columns: 2fr 1fr 1fr 1fr; gap: 48px; margin-bottom: 48px; }
  .footer-brand .footer-logo { font-family: var(--font-display); font-size: 1.4rem; color: var(--white); margin-bottom: 12px; }
  .footer-brand .footer-logo span { color: var(--gold); }
  .footer-brand p { font-size: .85rem; line-height: 1.7; }
  .footer-col h4 { color: var(--white); font-size: .78rem; letter-spacing: .14em; text-transform: uppercase; font-family: var(--font-mono); margin-bottom: 16px; }
  .footer-col ul { list-style: none; display: flex; flex-direction: column; gap: 10px; }
  .footer-col ul a { color: rgba(255,255,255,.55); text-decoration: none; font-size: .85rem; transition: color .3s; }
  .footer-col ul a:hover { color: var(--gold); }
  .footer-bottom { border-top: 1px solid rgba(255,255,255,.08); padding-top: 24px; display: flex; justify-content: space-between; align-items: center; flex-wrap: gap; }
  .footer-bottom p { font-size: .78rem; }
  .footer-bottom a { color: var(--gold); text-decoration: none; }
  .footer-phones { display: flex; gap: 12px; flex-wrap: wrap; }
  .footer-phones a { color: var(--gold-light); font-family: var(--font-mono); font-size: .8rem; text-decoration: none; }
  .footer-phones a:hover { color: var(--white); }

  /* ── FLOATING CTA ── */
  .floating-cta {
    position: fixed; bottom: 28px; right: 28px; z-index: 90;
    display: flex; flex-direction: column; gap: 12px; align-items: flex-end;
  }
  .float-btn {
    width: 52px; height: 52px; border-radius: 50%; display: grid; place-items: center;
    text-decoration: none; font-size: 1.3rem;
    box-shadow: 0 4px 24px rgba(0,0,0,.25);
    transition: transform .3s var(--ease), box-shadow .3s;
  }
  .float-btn:hover { transform: scale(1.12) translateY(-2px); box-shadow: 0 8px 32px rgba(0,0,0,.3); }
  .float-wa { background: #25D366; }
  .float-call { background: var(--gold); }

  /* ── ANIMATIONS ── */
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(28px); }
    to   { opacity: 1; transform: translateY(0); }
  }
  .reveal { opacity: 0; transform: translateY(32px); transition: opacity .7s var(--ease), transform .7s var(--ease); }
  .reveal.visible { opacity: 1; transform: none; }

  /* ── MOBILE ── */
  @media (max-width: 900px) {
    .why-grid { grid-template-columns: 1fr; gap: 32px; }
    .portfolio-grid { grid-template-columns: repeat(2, 1fr); }
    .process-steps { grid-template-columns: repeat(2,1fr); gap: 32px; }
    .process-steps::before { display: none; }
    .contact-wrapper { grid-template-columns: 1fr; gap: 48px; }
    .footer-grid { grid-template-columns: 1fr 1fr; }
  }
  @media (max-width: 640px) {
    .nav-links { display: none; }
    .hamburger { display: flex; }
    .nav-links.open { display: flex; flex-direction: column; position: absolute; top: 72px; left: 0; right: 0; background: var(--paper); padding: 20px 5vw 28px; border-bottom: 1px solid rgba(0,0,0,.1); gap: 1.2rem; }
    .portfolio-grid { grid-template-columns: 1fr; }
    .process-steps { grid-template-columns: 1fr; }
    .footer-grid { grid-template-columns: 1fr; }
    .form-row { grid-template-columns: 1fr; }
    .hero h1 { font-size: clamp(2.4rem, 10vw, 4rem); }
    .hero-stats { gap: 24px; }
    .cursor, .cursor-ring { display: none; }
  }

  /* Toast */
  .toast {
    position: fixed; bottom: 90px; left: 50%; transform: translateX(-50%) translateY(20px);
    background: var(--ink); color: var(--gold);
    padding: 14px 28px; border-radius: 2px;
    font-family: var(--font-mono); font-size: .8rem; letter-spacing: .1em;
    opacity: 0; transition: opacity .4s, transform .4s;
    pointer-events: none; z-index: 9999;
  }
  .toast.show { opacity: 1; transform: translateX(-50%) translateY(0); }
</style>
</head>
<body>

<div class="cursor" id="cursor"></div>
<div class="cursor-ring" id="cursorRing"></div>

<!-- NAV -->
<nav id="navbar">
  <a href="#" class="nav-logo">
    <div class="nav-logo-icon">L</div>
    <span class="nav-logo-text">Laxmi <span>Printers</span></span>
  </a>
  <ul class="nav-links" id="navLinks">
    <li><a href="#services">Services</a></li>
    <li><a href="#why">Why Us</a></li>
    <li><a href="#work">Portfolio</a></li>
    <li><a href="#process">Process</a></li>
    <li><a href="#contact" class="nav-cta">Get Quote →</a></li>
  </ul>
  <div class="hamburger" id="hamburger" onclick="toggleMenu()">
    <span></span><span></span><span></span>
  </div>
</nav>

<!-- HERO -->
<section class="hero">
  <div class="hero-bg"></div>
  <div class="hero-grid"></div>
  <div class="hero-content">
    <div class="hero-tag">Jaipur's Premier Print Studio</div>
    <h1>
      The Laxmi<br>
      <em>Printers</em>
      <span class="line2">&amp; Graphics</span>
    </h1>
    <p class="hero-sub">From concept to creation — logos, banners, wedding cards, books, and every print that tells your story with precision and craft.</p>
    <div class="hero-btns">
      <a href="#services" class="btn-primary">Explore Services ↓</a>
      <a href="tel:6378565668" class="btn-outline">📞 Call Now</a>
    </div>
    <div class="hero-stats">
      <div class="stat"><div class="stat-num">20<span>+</span></div><div class="stat-label">Service Types</div></div>
      <div class="stat"><div class="stat-num">4</div><div class="stat-label">Print Technologies</div></div>
      <div class="stat"><div class="stat-num">∞</div><div class="stat-label">Satisfaction</div></div>
    </div>
    <div class="hero-scroll-hint">
      <div class="scroll-line"></div>
      <span>Scroll</span>
    </div>
  </div>
</section>

<!-- MARQUEE -->
<div class="marquee-bar">
  <div class="marquee-track">
    <span>Logos</span><span>Visiting Cards</span><span>Letterheads</span><span>Brochures</span><span>Flex Banners</span><span>Stickers</span><span>Wedding Cards</span><span>Invitation Cards</span><span>Birthday Cards</span><span>Menu Cards</span><span>ID Cards</span><span>Certificates</span><span>Book Covers</span><span>Magazine Covers</span><span>Pamphlets</span><span>Newspapers Ads</span>
    <span>Logos</span><span>Visiting Cards</span><span>Letterheads</span><span>Brochures</span><span>Flex Banners</span><span>Stickers</span><span>Wedding Cards</span><span>Invitation Cards</span><span>Birthday Cards</span><span>Menu Cards</span><span>ID Cards</span><span>Certificates</span><span>Book Covers</span><span>Magazine Covers</span><span>Pamphlets</span><span>Newspapers Ads</span>
  </div>
</div>

<!-- SERVICES -->
<section class="services-bg" id="services">
  <div class="section-label reveal">Our Services</div>
  <h2 class="section-title reveal">Everything You Need to <em>Print & Stand Out</em></h2>

  <div class="services-grid reveal">
    <div class="service-card"><span class="service-icon">🎨</span><span class="service-name">Logo Design</span></div>
    <div class="service-card"><span class="service-icon">💳</span><span class="service-name">Visiting Card</span></div>
    <div class="service-card"><span class="service-icon">📄</span><span class="service-name">Letterhead</span></div>
    <div class="service-card"><span class="service-icon">📚</span><span class="service-name">Bill Books</span></div>
    <div class="service-card"><span class="service-icon">✉️</span><span class="service-name">Envelopes</span></div>
    <div class="service-card"><span class="service-icon">🖼️</span><span class="service-name">Posters</span></div>
    <div class="service-card"><span class="service-icon">📃</span><span class="service-name">Pamphlet</span></div>
    <div class="service-card"><span class="service-icon">📖</span><span class="service-name">Brochure</span></div>
    <div class="service-card"><span class="service-icon">🏷️</span><span class="service-name">Flex Banner</span></div>
    <div class="service-card"><span class="service-icon">🔖</span><span class="service-name">Sticker</span></div>
    <div class="service-card"><span class="service-icon">📋</span><span class="service-name">Handbill</span></div>
    <div class="service-card"><span class="service-icon">📅</span><span class="service-name">Calendar</span></div>
    <div class="service-card"><span class="service-icon">🎖️</span><span class="service-name">Certificate</span></div>
    <div class="service-card"><span class="service-icon">📕</span><span class="service-name">Book Cover</span></div>
    <div class="service-card"><span class="service-icon">💒</span><span class="service-name">Wedding Card</span></div>
    <div class="service-card"><span class="service-icon">🎉</span><span class="service-name">Invitation Card</span></div>
    <div class="service-card"><span class="service-icon">🎂</span><span class="service-name">Birthday Card</span></div>
    <div class="service-card"><span class="service-icon">🍽️</span><span class="service-name">Menu Card</span></div>
    <div class="service-card"><span class="service-icon">🪪</span><span class="service-name">ID Card</span></div>
    <div class="service-card"><span class="service-icon">📰</span><span class="service-name">Magazine Cover</span></div>
    <div class="service-card"><span class="service-icon">🗞️</span><span class="service-name">Newspaper Ad</span></div>
    <div class="service-card"><span class="service-icon">✨</span><span class="service-name">+ Much More</span></div>
  </div>

  <div class="printing-types reveal">
    <span class="print-chip chip-flex">Flex Printing</span>
    <span class="print-chip chip-digital">Digital Printing</span>
    <span class="print-chip chip-offset">Offset Printing</span>
    <span class="print-chip chip-screen">Screen Printing</span>
  </div>
</section>

<!-- WHY US -->
<section id="why">
  <div class="section-label reveal">Why Choose Us</div>
  <h2 class="section-title reveal">Trusted by Businesses<br><em>Across Jaipur</em></h2>
  <div class="why-grid">
    <div class="why-card reveal">
      <div class="why-num">01</div>
      <div>
        <div class="why-title">Expert Graphic Designers</div>
        <div class="why-text">Our team works with industry-standard tools — Adobe Illustrator, Photoshop, InDesign — to craft designs that are both beautiful and print-perfect.</div>
      </div>
    </div>
    <div class="why-card reveal">
      <div class="why-num">02</div>
      <div>
        <div class="why-title">All Printing Technologies</div>
        <div class="why-text">We offer Flex, Digital, Offset and Screen printing under one roof. Whatever your requirement, we have the right technology for it.</div>
      </div>
    </div>
    <div class="why-card reveal">
      <div class="why-num">03</div>
      <div>
        <div class="why-title">Fast Turnaround</div>
        <div class="why-text">Urgent order? No problem. We prioritize deadlines without compromising on the quality of your prints and design output.</div>
      </div>
    </div>
    <div class="why-card reveal">
      <div class="why-num">04</div>
      <div>
        <div class="why-title">Affordable Pricing</div>
        <div class="why-text">Competitive rates for every scale — whether you're a solo entrepreneur, a small business, or a large enterprise ordering in bulk.</div>
      </div>
    </div>
    <div class="why-card reveal">
      <div class="why-num">05</div>
      <div>
        <div class="why-title">End-to-End Service</div>
        <div class="why-text">From initial design concept to final print delivery, we handle everything. You get a single point of contact for all your printing needs.</div>
      </div>
    </div>
    <div class="why-card reveal">
      <div class="why-num">06</div>
      <div>
        <div class="why-title">Prime Jaipur Location</div>
        <div class="why-text">Located at Gangapol Gate near Tejaji Ka Mandir — easily accessible from the heart of Jaipur's old city and commercial districts.</div>
      </div>
    </div>
  </div>
</section>

<!-- PORTFOLIO -->
<section class="portfolio-bg" id="work">
  <div class="section-label reveal">Our Work</div>
  <h2 class="section-title reveal">Designs That <em>Leave an Impression</em></h2>
  <div class="portfolio-grid reveal">
    <div class="portfolio-item p-logo">
      <div class="portfolio-item-inner"><span class="p-label">Logo Design</span></div>
      <div class="portfolio-overlay"><span>Brand Identity & Logos →</span></div>
    </div>
    <div class="portfolio-item p-card">
      <div class="portfolio-item-inner"><span class="p-label">Visiting Cards</span></div>
      <div class="portfolio-overlay"><span>Business Cards & Stationery →</span></div>
    </div>
    <div class="portfolio-item p-banner">
      <div class="portfolio-item-inner"><span class="p-label">Flex Banners</span></div>
      <div class="portfolio-overlay"><span>Banners & Hoardings →</span></div>
    </div>
    <div class="portfolio-item p-wedding">
      <div class="portfolio-item-inner"><span class="p-label">Wedding Cards</span></div>
      <div class="portfolio-overlay"><span>Invitations & Event Cards →</span></div>
    </div>
    <div class="portfolio-item p-book">
      <div class="portfolio-item-inner"><span class="p-label">Book Covers</span></div>
      <div class="portfolio-overlay"><span>Books & Magazine Covers →</span></div>
    </div>
    <div class="portfolio-item p-poster">
      <div class="portfolio-item-inner"><span class="p-label">Posters</span></div>
      <div class="portfolio-overlay"><span>Posters & Advertisements →</span></div>
    </div>
  </div>
</section>

<!-- PROCESS -->
<section id="process">
  <div class="section-label reveal">How It Works</div>
  <h2 class="section-title reveal">Simple <em>4-Step</em> Process</h2>
  <div class="process-steps reveal">
    <div class="process-step">
      <div class="step-circle">📞<span class="step-num">1</span></div>
      <div class="step-title">Contact Us</div>
      <div class="step-desc">Reach out via WhatsApp, phone, or email. Tell us about your project and requirements.</div>
    </div>
    <div class="process-step">
      <div class="step-circle">💬<span class="step-num">2</span></div>
      <div class="step-title">Discuss & Quote</div>
      <div class="step-desc">We understand your needs, suggest the best options, and provide a transparent quote.</div>
    </div>
    <div class="process-step">
      <div class="step-circle">🎨<span class="step-num">3</span></div>
      <div class="step-title">Design & Review</div>
      <div class="step-desc">Our designers create your artwork. You review, give feedback, and we refine until perfect.</div>
    </div>
    <div class="process-step">
      <div class="step-circle">🖨️<span class="step-num">4</span></div>
      <div class="step-title">Print & Deliver</div>
      <div class="step-desc">We print with precision using the right technology and ensure timely delivery to you.</div>
    </div>
  </div>
</section>

<!-- CONTACT -->
<section id="contact" style="background:var(--section-alt)">
  <div class="section-label reveal">Contact Us</div>
  <h2 class="section-title reveal">Let's Bring Your <em>Vision to Print</em></h2>
  <div class="contact-wrapper">
    <div class="contact-info reveal">
      <p>We're based in Jaipur and ready to handle all your design and printing needs — big or small. Walk in, call us, or drop a message.</p>
      <div class="contact-items">
        <div class="contact-item">
          <div class="contact-icon">📞</div>
          <div class="contact-detail">
            <strong>Phone / WhatsApp</strong>
            <a href="tel:6378565668">6378565668</a>
            <a href="tel:8209544803">8209544803</a>
            <a href="tel:9799436759">9799436759</a>
          </div>
        </div>
        <div class="contact-item">
          <div class="contact-icon">✉️</div>
          <div class="contact-detail">
            <strong>Email</strong>
            <a href="mailto:sagersaini786@gmail.com">sagersaini786@gmail.com</a>
            <a href="mailto:ashusaini6221@gmail.com">ashusaini6221@gmail.com</a>
          </div>
        </div>
        <div class="contact-item">
          <div class="contact-icon">📍</div>
          <div class="contact-detail">
            <strong>Address</strong>
            <span>Gangapol Gate, Tejaji Ke Mandir Ke Pass,</span>
            <span>Jaipur – 302002, Rajasthan</span>
          </div>
        </div>
      </div>
    </div>
    <div class="contact-form reveal">
      <div class="form-title">Get a Quick Quote</div>
      <div class="form-row">
   <div class="contact-form reveal">
  <div class="form-title">Get a Quick Quote</div>

  <form action="/enquiry" method="POST">
    <div class="form-row">
      <div class="form-group">
        <label>Your Name</label>
        <input type="text" name="name" placeholder="Ramesh Sharma" required>
      </div>

      <div class="form-group">
        <label>Phone Number</label>
        <input type="tel" name="phone" placeholder="98XXXXXXXX" required>
      </div>
    </div>

    <div class="form-group">
      <label>Service Required</label>
      <select name="course" required>
        <option value="">Select a service...</option>
        <option>Logo Design</option>
        <option>Visiting Card</option>
        <option>Flex Banner</option>
        <option>Wedding Card</option>
        <option>Brochure</option>
        <option>Letterhead</option>
        <option>Bill Books</option>
        <option>Poster / Pamphlet</option>
        <option>ID Card</option>
        <option>Certificate</option>
        <option>Book / Magazine Cover</option>
        <option>Other</option>
      </select>
    </div>

    <div class="form-group">
      <label>Your Message</label>
      <textarea name="message" placeholder="Tell us about your project, quantity, deadline..."></textarea>
    </div>

    <button type="submit" class="form-submit">
      Send Message <span>→</span>
    </button>
  </form>
</div>
  </div>
</section>

<!-- MAP -->
<div class="map-section">
  <div class="map-container">
    <div class="map-placeholder">
      <div class="map-icon">📍</div>
      <p>Gangapol Gate, Jaipur – 302002</p>
      <small>Tejaji Ke Mandir Ke Pass</small><br>
      <a href="https://maps.google.com/?q=Gangapol+Gate+Jaipur" target="_blank" class="map-link">Open in Google Maps →</a>
    </div>
  </div>
</div>

<!-- FOOTER -->
<footer>
  <div class="footer-grid">
    <div class="footer-brand">
      <div class="footer-logo">The Laxmi <span>Printers</span> & Graphics</div>
      <p>Jaipur's trusted destination for all graphic design and printing needs. Quality, speed and affordability — all under one roof.</p>
      <div class="footer-phones" style="margin-top:16px">
        <a href="tel:6378565668">6378565668</a>
        <a href="tel:8209544803">8209544803</a>
        <a href="tel:9799436759">9799436759</a>
      </div>
    </div>
    <div class="footer-col">
      <h4>Services</h4>
      <ul>
        <li><a href="#services">Logo & Branding</a></li>
        <li><a href="#services">Visiting Cards</a></li>
        <li><a href="#services">Brochures</a></li>
        <li><a href="#services">Wedding Cards</a></li>
        <li><a href="#services">Banners & Flex</a></li>
      </ul>
    </div>
    <div class="footer-col">
      <h4>Printing</h4>
      <ul>
        <li><a href="#services">Flex Printing</a></li>
        <li><a href="#services">Digital Printing</a></li>
        <li><a href="#services">Offset Printing</a></li>
        <li><a href="#services">Screen Printing</a></li>
      </ul>
    </div>
    <div class="footer-col">
      <h4>Contact</h4>
      <ul>
        <li><a href="mailto:sagersaini786@gmail.com">sagersaini786@gmail.com</a></li>
        <li><a href="mailto:ashusaini6221@gmail.com">ashusaini6221@gmail.com</a></li>
        <li><a href="#contact">Get a Quote</a></li>
        <li><a href="https://maps.google.com/?q=Gangapol+Gate+Jaipur" target="_blank">Find Us on Map</a></li>
      </ul>
    </div>
  </div>
  <div class="footer-bottom">
    <p>© 2026 The Laxmi Printers & Graphics. All rights reserved.</p>
    <p>Made with ❤️ in <a href="#">Jaipur</a></p>
  </div>
</footer>

<!-- FLOATING CTA -->
<div class="floating-cta">
  <a href="https://wa.me/916378565668?text=Hello%2C+I%27d+like+to+get+a+quote+for+printing+services." target="_blank" class="float-btn float-wa" title="WhatsApp">💬</a>
  <a href="tel:6378565668" class="float-btn float-call" title="Call Now">📞</a>
</div>

<div class="toast" id="toast">✅ Message sent! We'll contact you shortly.</div>

<script>
  // Custom cursor
  const cursor = document.getElementById('cursor');
  const ring = document.getElementById('cursorRing');
  let mx=0,my=0,rx=0,ry=0;
  document.addEventListener('mousemove', e => { mx=e.clientX; my=e.clientY; cursor.style.left=mx+'px'; cursor.style.top=my+'px'; });
  function animRing(){ rx+=(mx-rx)*.12; ry+=(my-ry)*.12; ring.style.left=rx+'px'; ring.style.top=ry+'px'; requestAnimationFrame(animRing); }
  animRing();
  document.querySelectorAll('a,button,.service-card,.portfolio-item').forEach(el=>{
    el.addEventListener('mouseenter',()=>{ cursor.style.width='20px'; cursor.style.height='20px'; ring.style.width='56px'; ring.style.height='56px'; });
    el.addEventListener('mouseleave',()=>{ cursor.style.width='12px'; cursor.style.height='12px'; ring.style.width='36px'; ring.style.height='36px'; });
  });

  // Navbar scroll
  window.addEventListener('scroll', ()=>{
    document.getElementById('navbar').classList.toggle('scrolled', window.scrollY > 20);
  });

  // Mobile menu
  function toggleMenu(){ document.getElementById('navLinks').classList.toggle('open'); }

  // Reveal on scroll
  const revealEls = document.querySelectorAll('.reveal');
  const observer = new IntersectionObserver(entries=>{
    entries.forEach((e,i)=>{ if(e.isIntersecting){ setTimeout(()=>e.target.classList.add('visible'), i*80); } });
  },{ threshold:.12 });
  revealEls.forEach(el=>observer.observe(el));


</script>
</body>
</html>